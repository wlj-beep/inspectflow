# InspectFlow on-prem environment defaults
NODE_ENV=production

# Runtime ports
BACKEND_PORT=4000
FRONTEND_PORT=4173
FRONTEND_HOST=0.0.0.0

# PostgreSQL connections
DATABASE_URL=postgres://postgres@localhost:5432/inspectflow
DATABASE_URL_TEST=postgres://postgres@localhost:5432/inspectflow_test

# Auth/session controls
INSPECTFLOW_DEFAULT_PASSWORD=inspectflow
AUTH_SESSION_TTL_HOURS=12
AUTH_COOKIE_SECURE=false
ALLOW_LEGACY_ROLE_HEADER=false
AUTH_SSO_ENABLED=false
AUTH_SSO_PRINCIPAL_HEADER=x-forwarded-user
AUTH_SSO_ROLE_HEADER=x-forwarded-role
AUTH_SSO_AUTO_PROVISION=false
AUTH_SSO_DEFAULT_ROLE=Operator

# Analytics site-scope controls (BL-043)
ANALYTICS_MULTISITE_ENABLED=false
ANALYTICS_ALLOWED_SITE_IDS=default

# Analytics SLO thresholds (BL-045)
ANALYTICS_SLO_WINDOW_DAYS=30
ANALYTICS_SLO_MAX_BUILD_P95_MS=30000
ANALYTICS_SLO_MIN_SUCCESS_RATE=0.95
ANALYTICS_SLO_MAX_ERROR_RATE=0.05
ANALYTICS_SLO_STORAGE_BUDGET_MB=2048

# Extension SDK runtime policy defaults (BL-046)
EXT_RUNTIME_ENABLED=true
EXT_SDK_ALLOWED_HOOKS=technical_ops.summary,analytics.performance.slo,integration.run.completed
EXT_SDK_ALLOWED_CAPABILITIES=read_analytics_slo,read_technical_ops_summary,read_integration_runs
EXT_SDK_MAX_HOOKS=8
EXT_SDK_MAX_CAPABILITIES=8

# Backup defaults
BACKUP_ROOT=var/backups
BACKUP_RETENTION_DAYS=14

# Install/start behavior
INSPECTFLOW_SEED_ON_INSTALL=true

# Offline update bundle signing (BL-020)
# Preferred: point to a local file that contains only the signing key.
INSPECTFLOW_UPDATE_SIGNING_KEY_FILE=
# Fallback: inline key value (leave empty if using key file).
INSPECTFLOW_UPDATE_SIGNING_KEY=
# Minimum free disk required by preflight before applying update.
UPDATE_MIN_FREE_MB=1024
# If true, apply step runs npm install commands before migrate/build.
INSPECTFLOW_UPDATE_RUN_INSTALL=false
