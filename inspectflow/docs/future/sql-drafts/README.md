# SQL Drafts (Additive Only)

These are non-executed draft schemas for future modules.

Rules:
- Additive only (`CREATE TABLE IF NOT EXISTS`, additive indexes).
- No destructive changes (`DROP`, `ALTER ... DROP`, column type rewrites).
- Promote to real migrations only after owner approval and release planning.

Mapped backlog IDs:
- `BL-031`, `BL-032`, `BL-035`, `BL-039`, `BL-042`
