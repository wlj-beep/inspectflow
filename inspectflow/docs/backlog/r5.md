# R5 Backlog Shard

This shard is linked from the backlog index at `docs/backlog.md`.

## R5 Backlog (Security, Reliability, and Product Hardening)

| ID | Release | Stream | Module | Owner Team | Dependencies | Interface Contract | Priority Score | Acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BL-084 | R5 | PLAT | CORE | Team Atlas | BL-015, BL-082 | PLAT-SEC-v1 | 99 | Startup config validation enforces required env vars, hard-fails unsafe production boot, tightens CORS origin policy, and removes silent security fallbacks (including empty-pepper auth behavior). |
| BL-085 | R5 | PLAT | CORE | Team Atlas | BL-015, BL-016 | PLAT-AUTH-v1 | 98 | Auth hardening enforces server-side password complexity, isolates test-only auth bypass to non-production runtime guarantees, and emits audited admin password-reset actor/target events. |
| BL-086 | R5 | INT | INTEGRATION_SUITE | Team Bridge | BL-031, BL-069 | INT-SEC-v1 | 96 | Injection and abuse guards are added: SQL identifier whitelisting for analytics builder dynamic paths, endpoint URL credential rejection/encryption policy, and request-body depth limits. Confirmed injection points: `martBuilder.js` `snapshotMartTable` directly interpolates `orderBy` and `keyColumns` into SQL template strings — allowlist validation must cover all dynamic parameters in those functions. (RV-005) |
| BL-087 | R5 | PLAT | CORE | Team Atlas | BL-029 | PLAT-API-v1 | 94 | API contract standardization ships `/api/v1`, semantic domain error codes, consistent envelope shape, and canonical response casing without snake/camel drift. Review confirmed three conflicting error shapes currently in use: `{ error: "code" }`, `{ ok: false, error: { code, message } }`, and plain thrown exceptions — `martBuilder.js` error handling is a primary inconsistency example. All shapes must converge to one documented envelope. (RV-035) |
| BL-088 | R5 | PLAT | CORE | Team Atlas | BL-029, BL-052 | PLAT-CODE-v1 | 90 | Route/service architecture cleanup is enforced: shared validation utilities, imports/records business-logic extraction, auth event helper reuse, and elimination of `SELECT *` in operational query paths. Confirmed duplication targets for shared validators: `parsePositiveInteger`, `normalizeCalibrationDate`, and `normalizeOptionalText` are each implemented nearly identically across `dimensions.js`, `tools.js`, `search.js`, `imports.js`, and `records.js` — these must be consolidated into a single `backend/src/utils/validators.js` module. Service DI pattern must also be standardized: all service functions should accept an optional `db` parameter (matching the `faiPackages.js` pattern) so they are testable without module-level mocking. (RV-017, RV-018) |
| BL-089 | R5 | ANA | ANALYTICS_SUITE | Team Signal | BL-069 | ANA-MART-v3 | 88 | Serialization retry strategy is bounded (3-5 retries with backoff) and surfaces structured terminal errors with no partial-state leakage across analytics rebuild/refresh paths. |
| BL-090 | R5 | PLAT | CORE | Team Atlas | BL-053 | PLAT-DATA-v1 | 93 | Database integrity hardening adds critical indexes, CHECK constraints, duplicate-submission guards, nullable intent documentation, and audit-preserving delete behavior for compliance artifacts. |
| BL-091 | R5 | PLAT | CORE | Team Atlas | BL-090 | PLAT-DATA-v1 | 89 | Query-performance program establishes top-query `EXPLAIN ANALYZE` baselines, remediates sequential scans, and tracks regression budgets for critical endpoints. Confirmed sequential scan target: global search (`search.js`) fires 6 parallel ILIKE queries per request with leading `%` wildcards across jobs, records, issues, audit, tools, and users tables — these cannot use standard B-tree indexes; `tsvector`/GIN indexing on high-cardinality search columns is the recommended remediation. (RV-045) |
| BL-092 | R5 | PLAT | CORE | Team Atlas | BL-052, BL-087 | PLAT-TEST-v2 | 95 | Backend test modernization adopts session-auth fixtures and adds constraint, concurrency, mart-failure, import-recovery, authorization-boundary, edge-input, and performance regression suites. Confirmed cleanup target: `auth.test.js afterEach` currently sets `active=false` (soft deactivation) rather than deleting or rolling back test users, causing orphaned row accumulation across test runs — migration to transactional fixtures (begin/rollback per test) or per-suite DB teardown must be included. (RV-037) |
| BL-093 | R5 | OPS | CORE | Team Forge | BL-077 | OPS-FE-ARCH-v1 | 92 | Frontend architecture hardening delivers strict TypeScript adoption, lint/format/pre-commit gates, monolith decomposition, shared component library, state-store extraction, and removal of hardcoded seed datasets. Confirmed decomposition target: `InspectFlowApp.jsx` (~432KB) handles job workflows, instruction acknowledgments, analytics dashboards, and FAI orchestration in a single untestable file — it is the primary monolith target. Confirmed utility duplication: `fmtTs` and `normalizeOpNumber` are defined identically in both `mappers.js` and `InspectFlowApp.jsx` and must be consolidated. Confirmed hardcoded seed targets: `mocked.smoke.spec.js` uses `id: 1` and tool IDs `"t01"`–`"t14"` — replace with a test data factory pattern asserting on names/labels rather than internal IDs. (RV-019, RV-032, RV-038) |
| BL-094 | R5 | OPS | CORE | Team Forge | BL-093, BL-087 | OPS-FE-API-v1 | 90 | API client resilience layer adds timeout + abort handling, centralized 401 routing, retry with exponential backoff for retryable failures, request deduplication, and typed API error contracts. |
| BL-095 | R5 | OPS | CORE | Team Forge | BL-093, BL-094 | OPS-FE-UX-v1 | 88 | Frontend quality uplift completes a11y compliance basics, helper/component coverage, expanded Playwright scenarios, loading skeleton states, and optimistic-update rollback patterns. |
| BL-096 | R5 | QUAL | QUALITY_PRO | Team Helix | BL-075, BL-076 | QUAL-QMS-v3 | 91 | Competitive workflow expansion adds structured NCR/disposition, CAPA/8D lifecycle, MSA (Gage R&R), direct equipment ingestion channels, operator certification gating, and realtime quality alerting. |
