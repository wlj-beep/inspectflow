## Summary
- What changed:
- Why this change is needed:
- Scope boundaries (what is intentionally not included):

## Branch and Queue
- Branch name follows standard (`codex/*`, `feature/*`, `fix/*`): [ ] Yes
- Linked backlog/work item IDs (`BL-###`, ticket, or issue):

## Risk
- Risk level: Low / Medium / High
- User-facing impact:
- Data/schema impact:

## Testing
- [ ] `npm run test:api` (from `inspectflow/`)
- [ ] `npm run test:ui` (from `inspectflow/`)
- [ ] Manual checks from `inspectflow/docs/test-plan.md`
- Notes (include any skipped tests and why):

## Rollback
- Rollback approach:
- Backward compatibility notes:

## Documentation and Coordination
- [ ] Docs updated (`README`, `docs/*`) as needed
- [ ] Queue artifacts updated when applicable (`STATUS.md`, `WORKLOG.md`, `docs/backlog.md`)
- Notes:

## Artifact Hygiene
- [ ] No cache/build/test artifacts committed (`.npm-cache`, `node_modules`, `test-results`, coverage, dist)
- [ ] No secrets or local env files included
